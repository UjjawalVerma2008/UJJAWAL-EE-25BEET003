#include<stdio.h>

int main(

     int a,b,division;
        printf("Enter two integers: "); 
        scanf("%d %d",&a,&b);
        division=a/b;
        printf("Division of %d and %d is: %d",a,b,division);
        return 0;
)